/**
 * @fileoverview Auth redirect utility stub — will be replaced by Phase 2 auth agent.
 * For now, re-exports from the legacy module to keep the app bootable.
 */
export { getSafeAuthRedirect, getSafeAuthRedirectFromSearch, getAuthErrorFromLocation } from './authRedirect';
